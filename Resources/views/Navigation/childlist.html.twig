<div class="cmf-childlist">
    <ul>
        {% for url, title in children %}
            <li><a href="{{  path(routename, {'path': url}) }}">{{ title }}</a></li>
        {% endfor %}
    </ul>
</div>
