<ul>
    {% for entry in menu %}
        <li class="{% if entry.active %}active{% else %}inactive{% endif %} {% if entry.children %}{% if entry.children %}children{% else %}nochildren{% endif %}{% endif %}">
        {# if no children elem present at all, walker did not check for children #}
            <a href="{{ path(routename, {'path': entry.url}) }}">{{ entry.title }}</a>
            {% if entry.children %}
                {% include "SymfonyCmfNavigationBundle:Navigation:menu.html.twig" with {'menu': entry.children} %}
            {% endif %}
        </li>
    {% endfor %}
</ul>
